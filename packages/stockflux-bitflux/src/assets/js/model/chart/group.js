export default function(legend, nav, primary, secondary, xAxis) {
    return {
        legend: legend,
        nav: nav,
        primary: primary,
        secondary: secondary,
        xAxis: xAxis
    };
}
