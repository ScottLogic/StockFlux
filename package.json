{
  "name": "stockflux",
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --concurrency=2",
    "build": "lerna run build"
  }
}
